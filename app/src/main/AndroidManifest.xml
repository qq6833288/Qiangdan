<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.xlz.qiangdan" >

    <application
        android:allowBackup="true"
        android:icon="@mipmap/icon"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".activity.MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".activity.ContentActivity"/>
        <activity android:name=".activity.GrabbedOrderActivity"/>
        <activity android:name=".activity.ReleaseTaskActivity"/>
        <activity android:name=".activity.PlaceOrderActivity"/>
        <activity android:name=".activity.PersonalDataActivity"/>
    </application>

</manifest>
